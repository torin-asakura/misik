import styled         from '@emotion/styled'

import { Box }        from '@ui/layout'

import { baseStyles } from './container.styles'

const Container = styled(Box)(baseStyles)

export { Container }
